# Vision + dialogue/tool-use (action planning) + audio understanding
# ("STT") + audio generation ("TTS") -- all via Gemini's free tier
# (Flash models), no credit card required.
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
