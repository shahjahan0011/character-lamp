# Pinned to Python 3.11 (see .python-version): pybullet ships prebuilt
# manylinux wheels for cp310/cp311 but not cp312, which is Ubuntu 24.04's
# default python3. On macOS there are no prebuilt wheels at all regardless
# of version -- see scripts/install_pybullet_macos.sh.
pybullet>=3.2.6
pydantic>=2.6
numpy>=1.26
# Pinned below 5.0: opencv-python 5.x removed CascadeClassifier entirely
# (replaced by the DNN-based FaceDetectorYN), breaking the Haar-cascade
# approach this project uses for engagement detection.
opencv-python>=4.9,<5
sounddevice>=0.4.6
soundfile>=0.12
# webrtcvad is old and effectively unmaintained -- it imports pkg_resources
# unconditionally, which setuptools >=81 no longer bundles. Pinning
# setuptools below that keeps it working; if pkg_resources is ever
# actually removed upstream (Python packaging has been threatening this
# for a while), this needs a VAD replacement, not just a version bump.
webrtcvad>=2.0.10
setuptools<81
# One vendor for vision, dialogue/tool-use, "STT" (native audio input), and
# TTS (native audio output) -- Gemini's free tier (Flash models) needs no
# credit card and covers all of it, unlike OpenAI (no free tier at all,
# $5 purchase required) or Anthropic (no-card trial credit, but one-time,
# not an ongoing free tier).
google-genai>=0.3
python-dotenv>=1.0
psutil>=5.9
